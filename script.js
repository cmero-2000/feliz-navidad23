// Función que muestra el mensaje al hacer clic en el regalo
function showMessage() {
    // Obtiene el div del mensaje
    const messageDiv = document.getElementById("message");

    // Cambia el estilo para mostrar el mensaje
    messageDiv.style.display = "block";
}
